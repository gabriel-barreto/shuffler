import { styled } from '@/theme'

export const Field = styled('div', {
  alignItems: 'center',
  display: 'flex',
  flexDirection: 'column',
  gap: '$2',
  justifyContent: 'center',
  width: '100%'
})
